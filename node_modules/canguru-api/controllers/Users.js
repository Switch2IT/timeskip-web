'use strict';

var url = require('url');

var Users = require('./UsersService');

module.exports.createUser = function createUser (req, res, next) {
  Users.createUser(req.swagger.params, res, next);
};

module.exports.getCurrentUser = function getCurrentUser (req, res, next) {
  Users.getCurrentUser(req.swagger.params, res, next);
};

module.exports.listUsers = function listUsers (req, res, next) {
  Users.listUsers(req.swagger.params, res, next);
};

module.exports.parseJwt = function parseJwt (req, res, next) {
  Users.parseJwt(req.swagger.params, res, next);
};
